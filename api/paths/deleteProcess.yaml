get:
  tags:
    - taskmaster
  operationId: deleteProcess
  summary: delete un process
  parameters:
    - in: query
      name: name
      required: true
      schema:
        type: string
  responses:
    '200':
      description: 'OK'
    '404':
      description: "le process n'existe pas"

get:
  tags:
    - taskmaster
  operationId: listGroupProcess
  summary: Renvois la liste des process
  responses:
    '200':
      description: 'Tableau des process'
      content:
        application/json:
          schema:
            $ref: "../schemas/ListGroupProcess.yaml#/ListGroupProcess"

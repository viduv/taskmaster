post:
  tags:
    - taskmaster
  operationId: stop
  summary: Arette un process
  parameters:
    - in: query
      name: name
      required: true
      schema:
        type: string
  responses:
    '200':
      description: 'OK'
    '400':
      description: "Le process n'est pas demarré"
